# demo keeps answers deterministic and requires no external credentials.
LLM_MODE=demo
# Set LLM_MODE=remote only after providing a compatible endpoint and API key.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=
